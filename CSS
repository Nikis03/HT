/* Reset some basic styles */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: Arial, sans-serif;
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 100vh;
    background-color: #f5f5f5;
}

.container {
    text-align: center;
}

h1 {
    font-size: 2em;
    color: #333;
    margin-bottom: 20px;
}

.language-list, .level-selection {
    display: flex;
    flex-direction: column;
    align-items: center;
}

.language-button, .level-button, .back-button {
    background-color: #008080;
    color: #FFF;
    padding: 10px 20px;
    margin: 5px 0;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    font-size: 1.1em;
    width: 80%;
    max-width: 200px;
    text-align: center;
}

.language-button:hover, .level-button:hover, .back-button:hover {
    background-color: #006666;
}

/* Style for the Back to Home button */
.back-button {
    background-color: #555; /* Different color for "Back to Home" button */
    margin-top: 20px;
}
